import styled from '@emotion/styled'
import {css} from '@emotion/core'

const SubheadingBase = css`
  text-align: center;
  color: #a9a;
  margin: 2.7em;
  margin-top: 0.2em;
`;

const Subheading = styled.p`
  ${SubheadingBase}
`

export { Subheading }